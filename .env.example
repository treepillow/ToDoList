# Copy to server/.env and adjust. Never commit the real .env file.
PORT=3001
# Path to the SQLite database file (gitignored)
DB_PATH=./data/todos.db
# Origin allowed by CORS (Vite dev server)
CLIENT_ORIGIN=http://localhost:5173
